# Release notes — v3.3.0

**America — A Travel Guide** · 2026-07-16

Stability and quality release: multi-language tools, gallery correctness, modal scroll lock, unit conversion, and Gallery Manager hardening (local tooling).

---

## Highlights

- **Tip & sales tax state names** localize in English, Spanish, Chinese, and Japanese when you switch language.
- **Road-trip cost tool** converts mi ↔ km (and related economy / fuel fields) when distance units change, instead of re-reading the same numbers.
- **Gallery filters and lightbox** no longer break after language-related init; open lightbox captions refresh on language change.
- **Modal + language** no longer double-locks page scroll (stuck page after translating with a card open).
- **Gallery metadata** uses consistent state codes (`CA` vs `California`) for sort and display.

---

## Site (published)

### Travel Tools
- Localized U.S. state / D.C. names in the tip & sales tax selector (`en` / `es` / `zh` / `ja`); option values stay as codes so tax rates stay correct.
- Rebuilds the state list on language change and keeps the selected state.
- “No statewide sales tax” note only when the **state’s** table rate is zero—not when tax is manually set to 0%.
- Currency converter guards missing `fetch` / `AbortController`.
- World clock zone labels replace all underscores (e.g. `America/Los_Angeles`).

### Gallery
- Normalized all `data-state` / `data-location` values to postal-style forms (e.g. `San Francisco, CA`).
- Sort-by-state treats full names and abbreviations as the same state.
- Language switch refreshes placeholders, tile `aria-label`s, and an open lightbox caption/HD chrome.
- Lightbox focus trap includes “Load full quality”; full-image blob cache is capped to limit memory use.
- Grid markup indent cleaned up; missing `decoding="async"` fixed on one photo.

### Core UX / a11y
- Opening a modal while already open (language refresh) no longer stacks body scroll locks.
- Destination “saved” empty state falls back to the active language, not English only.
- Cursor trail cancels animation frames via the shared `cancelRaf` helper (polyfill-safe).

### i18n
- Japanese footer: fixed incorrect “swim on the open road” phrasing.
- Chinese footer: restored collaboration-with-AI clause for parity with other languages.

### Tests & packaging
- Playwright smoke: theme assertions tolerate OS dark-mode theme twins (`minimal`↔`glass`, `elegant`↔`luxury`).
- New E2E coverage: tools state selector shows Chinese names after switching to 中文.
- Package version **3.3.0**.
- `.gitignore`: geo cache files for the local photo tool.

---

## Local Gallery Manager (not shipped to GitHub Pages)

The manager under `tools/` remains **local-only** (gitignored, not deployed). If you keep a local copy, this release’s tooling fixes include:

- **Save** no longer overwrites hand-translated es/zh/ja captions with English.
- Concurrent photo adds: slug allocation + file/HTML/i18n writes under one lock.
- New queue items no longer default every photo to **coast**; category must be chosen (or applied as a default).
- Safer i18n key removal and escaped warning text in the queue UI.
- Locations abbreviated on save (`…, California` → `…, CA`).

Run: `python3 tools/gallery_manager.py` (see `tools/README.md`).

---

## Upgrade notes

1. Pull `main` or check out tag `v3.3.0`.
2. No build step — static site as before.
3. Clear site localStorage only if you want a clean preference slate (optional).
4. For photo workflow, keep using your local Gallery Manager; re-open it after updating the working tree if you maintain those scripts outside git.

---

## Verification

```bash
npx playwright test   # 8 smoke tests
```

Live site (GitHub Pages): use your usual Pages URL after the deploy workflow finishes.

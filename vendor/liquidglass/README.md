# @ybouane/liquidglass (vendored)

Pinned build of [`@ybouane/liquidglass`](https://github.com/ybouane/liquidglass) **1.0.3** (MIT).

- Runtime entry: `index.js` (bundled ESM; dynamic `import()` from the site when Liquid Glass is On).
- Re-vendor: `npm pack @ybouane/liquidglass` and copy `package/dist/*` here.

No site UI credit required by MIT; keep this note for maintainers.

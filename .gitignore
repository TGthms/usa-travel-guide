# macOS / editor noise
.DS_Store
**/.DS_Store
.vscode/
.idea/
*.swp
*~

# Node (dev-only package.json; no runtime build pipeline)
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Playwright (E2E smoke — local + CI artifacts)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Gallery Manager source (tools/gallery_manager.py, tools/README.md) IS tracked.
# Runtime junk and local-only helpers stay private / out of the public deploy.
Add Photos.command
**/__pycache__/
tools/__pycache__/
*.py[cod]
*.pyo
.Python
tools/.geo_cache.json
.geo_cache.json
**/.geo_cache.json

# Local temp / scratch from image workflows
images/gallery/tmp*
images/gallery/thumbs/tmp*
*.tmp
.cache/

# Local Cloudflare secrets
.dev.vars
.env
.env.*

# Trailer pipeline outputs (scripts + cards.html stay tracked)
trailer/music/
trailer/stills/
trailer/clips/
trailer/raw/
trailer/cards/*.png
trailer/plates/
trailer/*.mp4
